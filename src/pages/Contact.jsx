import React from 'react';
import { WorkInProgress, ContactForm } from '../components';

const Contact = () => {
    return (
        <div>
            <ContactForm />
        </div>
    );
};

export default Contact;
